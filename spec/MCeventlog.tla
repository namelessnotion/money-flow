----------------------------- MODULE MCeventlog -----------------------------
(* TLC model checking eventlog's own promises: that the contract says what
   it means to before anything is checked against it. *)
EXTENDS eventlog, MCvalues

=============================================================================
